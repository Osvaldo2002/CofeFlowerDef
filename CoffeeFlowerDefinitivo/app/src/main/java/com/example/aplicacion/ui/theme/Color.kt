package com.example.aplicacion.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta para un look profesional (blanco, negro, tonos café)

// COLORES NEUTROS BÁSICOS
val Negro = Color(0xFF1E1E1E)       // Un negro muy oscuro
val Blanco = Color(0xFFFFFFFF)      // Blanco puro
val GrisClaro = Color(0xFFEEEEEE)   // Gris muy claro, para fondos de tarjetas o separadores
val GrisMedio = Color(0xFF757575)   // Gris medio para texto secundario

// TONOS CAFÉ/MARRÓN
val CafePrincipal = Color(0xFF6F4E37)   // Un café fuerte, ideal para elementos primarios
val CafeClaro = Color(0xFFB08968)     // Un café más claro, para acentos
val CafeMuyClaro = Color(0xFFEFEBE9)  // Un café casi beige, para fondos suaves

// OTROS COLORES (para errores o acentos específicos si los necesitas)
val RojoError = Color(0xFFB00020)